# NOPE Completion Master Plan

Date: 2026-07-18
Starting commit: `f0098bb`
Stage 2 pre-stage commit: `6c6ea63c7729c495886ca0b0c140e7874e191302`
Stage 4 pre-stage commit: `418e4a15cf522b1ecf1ee32f359b7ffd9c52d193`
Stage 12 pre-stage commit: `6267b3c6b05c0671d2d311876b3b25f3e4885dd1`

This plan is the gate for the NOPE 100% completion program. It uses the forensic audit as the starting point and keeps implementation work ordered by the required stages. A row is not complete until the listed acceptance tests have actually run and the evidence/commit columns are filled.

| Capability | Current audited percentage | Current implementation files | Existing tests | Defect or missing behavior | Exact required implementation | Exact acceptance tests | External dependencies | Target phase | Final status | Evidence | Completion commit |
| --- | ---: | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Benchmark correctness | 100% locally achievable | `apps/api/nope_api/benchmarks.py`, `apps/api/nope_api/rules_engine.py`, `apps/api/nope_api/finding_validation.py`, `benchmarks/fixtures/nope-benchmark-v1`, `benchmarks/expected/nope-benchmark-v1.expected.json`, `security-packs/nope-core-rules.json`, `.github/workflows/benchmarks.yml` | `apps/api/tests/test_phase12_benchmarks.py`, `apps/api/tests/test_finding_validation.py`, `apps/api/tests/test_scanners.py`, full backend suite | Earlier Stage 1 work covered only 22 expected categories; the original completion prompt required 41 categories plus negative controls and scanner/Qwen failure-mode tests. | Completed Stage 1 review: expanded the fixture/expected manifest to all 41 categories, added deterministic rules, fixed same-line dedupe, fixed auth/policy validation, excluded benchmark control metadata from rule scans, added parser/failure-mode/reproducibility tests, upgraded metrics/Markdown output, and added CI scanner-only artifact upload. | Docker scanner-only benchmark passes; Docker scanner-plus-Qwen benchmark passes; benchmark tests pass; full backend suite passes; precision/recall/F1 targets met; CI workflow uploads scanner-only JSON/Markdown artifacts. | Local scanner CLIs and optional Qwen runtime; scanner-plus-Qwen CI activation requires model/GPU secrets and runner capability. | Stage 1 | Complete | scanner-only: 41/41 TP, 0 FP/FN, precision/recall/F1 `1.000`; scanner-plus-Qwen: 41/41 TP, 0 FP/FN, precision/recall/F1 `1.000` | Stage 1 completion commits |
| Scan event and progress durability | 100% locally achievable | `apps/api/nope_api/queue.py`, `apps/api/nope_api/scan_engine.py`, `apps/api/nope_api/storage.py`, `apps/api/nope_api/main.py`, `apps/api/nope_api/models.py`, `apps/api/migrations/0003_scan_events.sql`, `apps/api/alembic/versions/0003_scan_events.py`, `apps/web/app/api/scan-events/[scanId]/route.ts` | `apps/api/tests/test_queue.py`, `apps/api/tests/test_stage2_scan_events.py`, full backend suite | Earlier defect fixed: completed scans no longer depend on mutable `scan.stages` snapshots and no longer return zero events. | Implemented durable `scan_events` with sequence/idempotency constraints; persisted scan/stage/scanner/retry/cancellation/worker/report/timeout/partial/failed/completed/Qwen transitions; added monotonic progress guard, redacted error details, paginated/incremental API replay, snapshot backfill, and frontend proxy pagination. | Focused Stage 2 suite passes; full backend suite passes; frontend typecheck/build passes; Docker stack applies `0003_scan_events`; live completed scan `scan_3b0b1418da3447fe` returned 26 ordered events from `scan_created` through `report_completed`. | None. | Stage 2 | Complete | `149 passed, 2 warnings`; Docker health ok; live event smoke non-empty ordered timeline | Stage 2 completion commit |
| Worker and sandbox security hardening | 100% locally achievable | `docker-compose.yml`, `apps/api/nope_api/runner.py`, `apps/api/nope_api/sandbox.py`, `apps/api/nope_api/security.py`, `apps/api/nope_api/ingestion.py`, `apps/api/nope_api/url_scanner.py`, `apps/api/nope_api/config.py`, `apps/api/nope_api/scan_engine.py` | `apps/api/tests/test_phase10_sandbox.py`, `apps/api/tests/test_security.py`, `apps/api/tests/test_stage3_security_hardening.py`, full backend suite | Original defect fixed: worker no longer runs as root in Compose and no longer has broad Docker socket access; hostile archive/URL/sandbox matrix is covered. | Implemented Stage 3 runner architecture: `nope-runner` is the only Docker-socket holder; general worker is non-root and socketless; sandbox execution is token-authenticated and limited to existing workspaces under `NOPE_TEMP_ROOT`; scanner execution remains in the normal worker without Docker daemon access; sandbox manifests are reduced to controlled job specs with image/command/network/env/mount allowlists; read-only workspace mounts, tmpfs work dirs, no-new-privileges, cap-drop, CPU/memory/PID/log/time limits, cleanup, and network-off ordinary workflows are enforced. ZIP ingestion and URL scanning are hardened for hostile archive and SSRF/rebinding/redirect/response-size cases. | Compose inspection proves worker has no Docker socket and uid `100(nope)`; runner has the isolated socket boundary; hostile ZIP, symlink, hardlink/special file, bomb, deep nesting, duplicate path, arbitrary image, arbitrary command, arbitrary env/mount/network, SSRF localhost/metadata/unresolved host, unsafe redirects, and port/response limits tests pass; full regression and Docker health pass; live runner sandbox smoke passes with cleanup. | Local Docker daemon for runner activation; rootless Docker or a separate runner host remains an optional production hardening path, not required after socket isolation for this local stage. | Stage 3 | Complete | `155 passed, 2 warnings`; focused Stage 3/security suite `20 passed`; compileall passed; Docker compose config passed; Docker stack rebuilt/healthy; worker socket absent; runner socket present; live runner smoke passed; no leftover sandbox containers/networks | Stage 3 completion commit |
| Dynamic scanning and ZAP | 100% locally achievable | `apps/api/nope_api/sandbox.py`, `apps/api/nope_api/url_scanner.py`, `apps/api/nope_api/scan_engine.py`, `apps/api/nope_api/reports.py`, Docker Compose runner/ZAP images, coverage UI | `apps/api/tests/test_phase10_sandbox.py`, `apps/api/tests/test_stage4_dynamic_zap.py`, full backend suite | Fixed: ZAP/dynamic testing no longer remains decorative for supported manifests; ZAP JSON/version/config/artifacts are captured and parsed; readiness, version, artifact, auth-state, and failure states are proven. | Completed Stage 4: supported exactly four scan modes: static repository scans with honest dynamic not-applicable coverage when no manifest exists; authorized URL scans using existing non-destructive scoped URL checks only; repository build-and-run dynamic scans for manifest-declared Node (`node server.js`) and Python (`python app.py` or `python -m http.server 8080 --bind 0.0.0.0`) apps inside isolated containers; and combined repository-plus-URL scans that run static repository evidence plus scoped URL checks while ZAP scans only the internal manifest app. Implemented real ZAP baseline execution on a runner-created private Docker network, readiness probes, timeout and teardown handling, ZAP version/config/raw JSON capture, alert parsing/normalization/deduplication, correlation fingerprints, bounded artifacts, and explicit unauthenticated/partial/skipped/unsupported/failed-build/failed-startup/readiness/timeout/crash coverage. | Supported Node and Python fixtures produce private-network ZAP runs, parsed alerts, raw artifacts, and dynamic coverage; URL authorization rejects unauthorized external targets; unsupported stacks and build/start/readiness/ZAP failures report accurately; static-only scans do not mark ZAP complete; reports/UI expose dynamic coverage; focused Stage 4, sandbox/security, URL, report, full backend, frontend build/typecheck, and live Docker ZAP smoke tests pass. | Local Docker daemon for `nope-runner`, allowlisted Node/Python/ZAP images, and authorized user-supplied URL targets. Full browser login automation for authenticated crawling is not added in Stage 4; authentication state is recorded and unauthenticated/partial coverage is explicit. | Stage 4 | Complete | Focused Stage 4/sandbox suite: `18 passed`; full backend suite: `164 passed, 2 warnings`; typecheck passed; Next build passed with existing autoprefixer warning; compileall passed; live Python and Node fixtures both produced `OWASP ZAP passed` with internal targets, raw artifacts, and Partial unauthenticated coverage; no leftover sandbox containers/networks. | Stage 4 completion commit |
| npm audit plugin | 100% locally achievable | `apps/api/nope_api/scanners.py`, Docker API image | `apps/api/tests/test_scanners.py` | Fixed: `npm audit` is now a first-class scanner with package-lock applicability, command safety, JSON parsing, package/advisory fields, artifact capture through scanner-run raw output, and unavailable state. | Implemented lockfile-only npm audit plugin using `npm audit --json --package-lock-only --ignore-scripts`, parser, severity/package/advisory/fix normalization, metadata, coverage, and Docker npm availability. | Parser fixture, controlled-command execution test, unavailable test, capability metadata smoke, scanner artifact regression, full backend suite. | npm registry/advisory service when applicable. | Stage 5 | Complete | `175 passed, 2 warnings`; Docker health reports npm audit installed; capability smoke reports package-lock marker and JSON output | Stage 5 completion commit |
| pnpm audit plugin | 100% locally achievable | `apps/api/nope_api/scanners.py`, Docker API image | `apps/api/tests/test_scanners.py` | Fixed: `pnpm audit` is now first-class with pnpm-lock applicability, JSON parsing, package/advisory fields, and unavailable state. | Implemented pnpm audit plugin using `pnpm audit --json`, parser for advisory payloads, metadata, coverage, controlled command, and Docker pnpm availability. | Parser fixture, controlled-command execution test, unavailable test, capability metadata smoke, full backend suite. | npm registry/advisory service when applicable. | Stage 5 | Complete | `175 passed, 2 warnings`; Docker health reports pnpm audit installed; capability smoke reports pnpm-lock marker and JSON output | Stage 5 completion commit |
| yarn audit plugin | 100% locally achievable | `apps/api/nope_api/scanners.py`, Docker API image | `apps/api/tests/test_scanners.py` | Fixed: Yarn audit is now first-class with Yarn classic and Berry command/output handling. | Implemented yarn audit plugin with lockfile applicability, classic `yarn audit --json` and Berry `yarn npm audit --json` command selection, NDJSON/object parsers, metadata, coverage, and Docker yarn availability. | Parser fixture for NDJSON, controlled-command execution test, unavailable test, capability metadata smoke, full backend suite. | npm registry/advisory service when applicable. | Stage 5 | Complete | `175 passed, 2 warnings`; Docker health reports yarn audit installed; capability smoke reports yarn.lock marker and JSON output | Stage 5 completion commit |
| pip-audit plugin | 100% locally achievable | `apps/api/nope_api/scanners.py`, `apps/api/requirements.txt`, Docker API image | `apps/api/tests/test_scanners.py` | Fixed: Python dependency auditing is first-class for requirements/project manifests with JSON parsing. | Implemented pip-audit plugin using JSON output against requirements/project manifests, parser for dependency vulnerabilities, fixed versions, aliases, metadata, coverage, and Docker pip-audit availability. | Parser fixture, controlled-command execution test, unavailable test, capability metadata smoke, full backend suite. | PyPI/advisory services when applicable. | Stage 5 | Complete | `175 passed, 2 warnings`; Docker health reports pip-audit installed; container command smoke matches pip-audit 2.10.1 CLI | Stage 5 completion commit |
| .NET package audit plugin | 100% locally achievable | `apps/api/nope_api/scanners.py` | `apps/api/tests/test_scanners.py` | Fixed: .NET vulnerable package auditing is represented as a first-class plugin with honest unavailable state when SDK is absent. | Implemented plugin for official `dotnet package list --vulnerable --include-transitive --format json`, project/solution applicability, parser, metadata, coverage, and explicit unavailable state when .NET SDK is absent. | Parser fixture, controlled-command execution test, unavailable test, capability metadata smoke, full backend suite. | .NET SDK and NuGet advisory data when installed. | Stage 5 | Complete | `175 passed, 2 warnings`; Docker health reports `.NET package audit` unavailable because `dotnet` is absent, as required | Stage 5 completion commit |
| cargo audit plugin | 100% locally achievable | `apps/api/nope_api/scanners.py` | `apps/api/tests/test_scanners.py` | Fixed: Rust dependency auditing is represented as a first-class plugin with honest unavailable state when cargo-audit is absent. | Implemented Cargo.lock applicability, `cargo audit --json` controlled command, cargo-audit-specific health check, parser, advisory/fixed-version normalization, metadata, coverage, and explicit unavailable state. | Parser fixture, controlled-command execution test, unavailable test, capability metadata smoke, full backend suite. | Rust/cargo-audit advisory DB when installed. | Stage 5 | Complete | `175 passed, 2 warnings`; Docker health reports cargo audit unavailable because cargo/cargo-audit is absent, as required | Stage 5 completion commit |
| govulncheck plugin | 100% locally achievable | `apps/api/nope_api/scanners.py` | `apps/api/tests/test_scanners.py` | Fixed: Go reachability-aware vulnerability auditing is represented as a first-class plugin with honest unavailable state when govulncheck is absent. | Implemented go.mod applicability, `govulncheck -json ./...` controlled command, NDJSON parser, advisory/package/trace normalization, metadata, coverage, and explicit unavailable state. | Parser fixture, controlled-command execution test, unavailable test, capability metadata smoke, full backend suite. | Go toolchain/govulncheck and Go vulnerability database when installed. | Stage 5 | Complete | `175 passed, 2 warnings`; Docker health reports govulncheck unavailable because CLI is absent, as required | Stage 5 completion commit |
| composer audit plugin | 100% locally achievable | `apps/api/nope_api/scanners.py` | `apps/api/tests/test_scanners.py` | Fixed: PHP Composer dependency auditing is represented as a first-class plugin with honest unavailable state when Composer is absent. | Implemented composer.lock applicability, `composer audit --format=json --locked` controlled command, parser, advisory/package/fix normalization, metadata, coverage, and explicit unavailable state. | Parser fixture, controlled-command execution test, unavailable test, capability metadata smoke, full backend suite. | Composer and Packagist/FriendsOfPHP advisory data when installed. | Stage 5 | Complete | `175 passed, 2 warnings`; Docker health reports composer audit unavailable because Composer is absent, as required | Stage 5 completion commit |
| bundler-audit plugin | 100% locally achievable | `apps/api/nope_api/scanners.py` | `apps/api/tests/test_scanners.py` | Fixed: Ruby Bundler auditing is represented as a first-class plugin with honest unavailable state when bundler-audit is absent. | Implemented Gemfile.lock applicability, `bundle-audit check --format json --no-update` controlled command, parser, advisory/package/fix normalization, metadata, coverage, and explicit unavailable state. | Parser fixture, controlled-command execution test, unavailable test, capability metadata smoke, full backend suite. | Ruby/bundler-audit advisory DB when installed. | Stage 5 | Complete | `175 passed, 2 warnings`; Docker health reports bundler-audit unavailable because CLI is absent, as required | Stage 5 completion commit |
| Canonical finding schema and provenance | 100% locally achievable | `apps/api/nope_api/models.py`, `rules_engine.py`, `scanners.py`, `storage.py`, migrations | `apps/api/tests/test_stage6_findings_lifecycle.py`, `test_phase4_findings.py`, `test_scanners.py` | Fixed: Finding JSON now has an enforced canonical contract for schema version, canonical fingerprint, original scanner fingerprint, source metadata, correlation evidence, and stable recurrence identity. | Added schema version/source metadata fields, canonical fingerprint assignment, original fingerprint preservation, duplicate source/evidence preservation, and DB indexes for project/fingerprint/status/concurrency. | Stable fingerprint, duplicate-source merge, scanner provenance, and persistence tests pass. | None. | Stage 6 | Complete | Focused Stage 6: `7 passed`; finding/scanner/drift regression: `42 passed`; full backend: `182 passed, 2 warnings`; frontend typecheck/build passed; Docker health ok with `0004_finding_lifecycle` applied | Stage 6 completion commit |
| Lifecycle transitions and audit history | 100% locally achievable | `apps/api/nope_api/lifecycle.py`, `storage.py`, `main.py`, migrations | `apps/api/tests/test_stage6_findings_lifecycle.py`, `test_phase7_findings.py` | Fixed: suppression no longer defaults missing reasons; general transition API validates states and records durable lifecycle/audit history. | Enforced New, Confirmed, Fixing, Fixed, Verified, False Positive, Accepted Risk, Suppressed, Reopened, and Reintroduced transitions; persisted actor, reason, timestamp, previous/new state, optimistic version, and audit log rows. | Valid/invalid transition, audit-history, unauthorized, and concurrency tests pass. | Authenticated owner token. | Stage 6 | Complete | Focused Stage 6: `7 passed`; full backend: `182 passed, 2 warnings`; frontend typecheck/build passed; Docker health ok with web/API/worker/runner/AI dependencies up | Stage 6 completion commit |
| Suppression expiry, recurrence, and reintroduction | 100% locally achievable | `storage.py`, `drift.py`, reports/history APIs | `apps/api/tests/test_stage6_findings_lifecycle.py`, `test_phase4_findings.py`, `test_phase8_drift.py` | Fixed: expired suppressions reopen during scan reads; suppression metadata is required; recurrence/reintroduction uses durable lifecycle state and is surfaced in reports/history. | Required suppression reason/actor/timestamp/scope/optional expiry, expire suppressions on scan/detail/list access, reopen automatically, detect recurrence and reintroduction from prior lifecycle state, and include lifecycle data in reports/history/filters. | Expiry, reintroduced, recurrence, report/history/filter tests pass. | None. | Stage 6 | Complete | Focused Stage 6: `7 passed`; finding/drift regression: `42 passed`; full backend: `182 passed, 2 warnings`; frontend typecheck/build passed; Docker health ok with `0004_finding_lifecycle` applied | Stage 6 completion commit |
| Qwen and RAG reliability, cache, UX | 100% locally achievable | `apps/api/nope_api/ai.py`, `rag.py`, `storage.py`, `main.py`, `apps/api/migrations/0005_ai_actions.sql`, `apps/web/components/ai-finding-actions.tsx`, `apps/web/app/api/ai/finding-action/route.ts` | `apps/api/tests/test_stage7_qwen_rag.py`, `test_phase5_qwen.py`, `test_phase6_rag.py`, full backend suite, frontend typecheck/build | Fixed: finding AI actions no longer depend on a blocking browser request or process-memory cache; unchanged evidence can use a durable 24-hour cache after API restarts; malformed JSON gets bounded retries and repair/fallback; RAG graph context is finding-centered instead of whole-graph. | Implemented durable `ai_action_jobs` and `ai_action_cache`; support Explain, Challenge, Fix, Regression Test, and Patch Review; persist queued/running/completed/failed/cancelled; key cache by fingerprint/action/model/quantization/prompt/RAG/evidence/settings; redact cache/job outputs; keep whole repositories out of prompts; use lexical, symbol, graph, route, and finding-centered retrieval with provenance and limits; expose async polling/cancellation and cached/latency UI states. | Stage 7 focused tests cover all actions, structured retry/repair, durable cache restart, invalidation, cancellation, redaction, prompt-injection controls, and cache key factors; Qwen/RAG regressions pass; Docker migration/health and VRAM check pass. | Local GGUF runtime for live generation; first uncached latency is bounded by local GPU/model speed, while repeated equivalent actions are cache hits. | Stage 7 | Complete | Pre-stage SHA `c2ea6916497ced32481ac27ed5f919ccd661c7cb`; focused Stage 7/Qwen/RAG tests `18 passed`; full backend `187 passed, 2 warnings`; frontend typecheck/build passed; Docker health ok with `0005_ai_actions` applied; `nope-ai` configured with `--n-gpu-layers 28`, target `5000 MB`, current idle VRAM sample `0/6144 MiB`, live logs show 6.86-9.32 tok/s uncached generation | Stage 7 completion commit |
| Browser E2E, accessibility, visual regression | 100% locally achievable | `apps/web/app`, `apps/web/components`, `apps/web/tests/e2e`, `apps/web/playwright.config.ts`, frontend package config, `.github/workflows/browser-e2e.yml` | Playwright browser matrix, axe route checks, visual snapshots, frontend lint/typecheck/build, focused backend regression | Fixed: the app now has deterministic browser coverage instead of relying only on lint/build; modal focus trapping, sidebar keyboard activation, scan cancellation/retry, Qwen action UI states, and route visual baselines are proven in browser tests. | Completed Stage 8: added Playwright with deterministic `NOPE_E2E_FIXTURE=1` data for landing, login/logout, session persistence, project creation, ZIP upload, scan start/progress/completion/partial/failure/cancellation/retry/delete, findings/detail tabs/lifecycle states/Qwen actions, reports, baselines, drift, settings, error and empty states, assets, coverage, attack map, and mobile navigation. Covered exact widths `1440`, `1280`, `1024`, `768`, `390`, and `360`; added axe checks on `/`, `/login`, overview, scans folders, scan folder, findings, attack-map, coverage, assets, reports, and settings; froze animations and masked volatile data for visual snapshots. | Browser suite passes locally with `pnpm web:e2e`; axe reports no serious/critical violations on major routes; keyboard focus, dialog trap, Escape, reduced motion, icon-button names, and mobile collapse pass; screenshots are generated and stable; web lint/typecheck/build pass; CI command is documented and workflow added. | Chromium browser runtime in CI/local Playwright; fixture mode avoids depending on mutable local scan state while still exercising Next routes/forms. | Stage 8 | Complete | Pre-stage SHA `cffae8eab0467935be5b9e3ca21cc5d54fc02f2a`; `pnpm web:e2e` passed `109 passed, 35 skipped` in 25.4m; visual snapshot update passed `13 passed, 35 skipped`; visual comparison passed `13 passed, 35 skipped`; `pnpm web:typecheck`, `pnpm --dir apps/web lint`, and `pnpm web:build` passed; focused backend Stage 7/Stage 2 regression passed `10 passed, 2 warnings`. | Stage 8 completion commit |
| GitHub integration completion | 100% locally achievable / external activation blocked without credentials | `apps/api/nope_api/github.py`, `apps/api/nope_api/main.py`, `apps/api/nope_api/storage.py`, `apps/api/nope_api/settings_contracts.py`, GitHub settings UI/routes/docs | `apps/api/tests/test_phase11_settings_github.py`, `apps/api/tests/test_stage9_github_integration.py`, full backend suite, frontend lint/typecheck/build, targeted settings browser/axe tests | Fixed: GitHub is no longer a blocked-only contract locally. It now has encrypted token storage, state validation, repository listing, archive retrieval, commit SHA capture, snapshot creation, disconnect/revocation behavior, policy checks, and protocol tests. | Completed Stage 9 local scope: kept the existing GitHub contract model and encrypted-secret storage, then completed a secure GitHub App/OAuth-compatible token activation path with state/CSRF validation, callback validation, repository listing, branch/default-branch/SHA discovery, least-privilege archive retrieval, repository snapshot creation, expiry/revocation/disconnect/audit logging, ownership isolation, archive size/file-count/submodule/LFS policy checks, secret-safe logs/remotes/reports, blocked-state UX when credentials are absent, and protocol tests using dependency-injected local fake GitHub HTTP services. PR patch workflow was not added because it is not activated in the current product scope. | Credential encryption and redaction tests pass; state/callback/revocation/disconnect/ownership tests pass; fake GitHub protocol tests prove repository listing, branch selection, default branch detection, commit SHA capture, archive retrieval, snapshot creation, and policy failures; no credentials appear in report output or git remotes; blocked-state tests pass without real credentials; full relevant backend/frontend regression passes. | Real GitHub App/OAuth/token credentials are required only for private repository activation against github.com; local completion uses a controlled fake GitHub server and marks external activation blocked if no real credentials are supplied. | Stage 9 | Complete | Pre-stage SHA `d305eab0e379ef0b97f93c66648d22b6ae983f22`; focused Stage 9/Phase 11 GitHub suites `7 passed, 2 warnings`; full backend suite `191 passed, 2 warnings`; `compileall` passed; `pnpm web:typecheck`, `pnpm --dir apps/web lint`, and `pnpm web:build` passed; targeted settings logout flow `6 passed`; targeted settings axe route `6 passed`. | Stage 9 completion commit |
| Persistence, drift, and reporting hardening | 100% locally achievable | `apps/api/nope_api/storage.py`, `drift.py`, `reports.py`, `main.py`, persistence/report/drift docs | `apps/api/tests/test_persistence.py`, `test_phase8_drift.py`, `test_phase9_pdf_reports.py`, full backend suite, frontend lint/typecheck/build | Fixed: report generation now has durable running/failed/completed state, failed rows do not serve stale bodies, retries are explicit, artifact metadata is owner-scoped, retention cleanup is owner-scoped, restart survival is covered, and drift includes version-level changes. | Completed Stage 10 from pre-stage SHA `ec65556383c679741e89de3461670b593723a1cb`: durable report generation status rows with redacted retryable failure state, owner-scoped artifact/report access, retention cleanup with audit counts, restart-survival contracts for scan/report/baseline/drift/AI durable state, latest-vs-previous/latest-vs-baseline/arbitrary comparison coverage, expanded drift categories for route/dependency/CVE/secret/RLS/CORS/header/tracker/public asset/scanner coverage/scanner version/rule version/model version/RAG version plus severity/confidence changes, stable fingerprint assertions, conservative incremental-scan metadata, redaction in persisted reports, and honest partial/failed/missing coverage representation. | Fresh DB migration smoke, restart-survival, large report, drift same/different project, arbitrary comparison, report failure/retry, artifact/report ownership, retention cleanup, redaction, compile, full backend regression, and frontend lint/typecheck/build passed. | MinIO/Postgres/Redis plus durable Qwen cache/job tables. | Stage 10 | Complete | Focused Stage 10 persistence/drift/report suite: `18 passed, 2 warnings`; `python -m compileall apps/api/nope_api apps/worker/worker.py` passed; full backend suite: `195 passed, 2 warnings`; `pnpm web:typecheck`, `pnpm --dir apps/web lint`, and `pnpm web:build` passed. | Stage 10 completion commit |
| NOPE self-security hardening | 100% locally achievable with documented scanner residual | `apps/api/nope_api/auth.py`, `apps/api/nope_api/config.py`, `apps/api/nope_api/main.py`, `apps/api/requirements.txt`, `docker/api.Dockerfile`, `docker-compose.yml`, security/API/database docs | `apps/api/tests/test_stage11_self_security.py`, `apps/api/tests/test_api_auth.py`, `apps/api/tests/test_security.py`, full backend suite, frontend lint/typecheck/build, dependency/container scans | Stage 11 pre-stage SHA `46b6f40487458e005adbf9bd9d4fdaf309204498`. Fixed: bearer session tokens were stored raw in `local_sessions`; login brute-force limits were process-memory only; `/health` exposed scanner/AI/model/runtime/sandbox/database details without auth; CORS allowed broad methods/headers; no shared request-body guard existed; browser-origin state-changing requests had no explicit Origin/CSRF guard; Compose lacked explicit service limits/read-only runtime hardening; dependency/container scan evidence was missing. | Implemented SHA-256 session-token storage with legacy-token compatibility, stronger PBKDF2 iteration control, Redis-backed shared login throttling with process-local fallback, strict allowed-origin CORS, state-changing Origin guard, API request-size rejection, common security headers, sanitized public `/health`, authenticated `/api/health/details`, non-root/read-only/cap-drop/no-new-privileges/tmpfs/resource limits for general services, pinned scanner/audit dependencies, removed unused `wheel` from the API runtime image, and documented residual local Compose/runner/protobuf risks. | Focused Stage 11/auth/security tests pass; full backend regression passes; frontend lint/typecheck/build pass; `docker compose config` passes; `pip check` inside rebuilt API image is clean; `pip-audit` fixes prior `mcp`, `click`, and `aiohttp` advisories and reports only the documented Semgrep/OpenTelemetry `protobuf` residual `CVE-2026-0994`; Trivy production-code scan is clean when intentional vulnerable fixtures are excluded; Docker stack refresh/health verification is required before final dossier. | Redis/Postgres/Docker/scanner audit tools; local Compose publishes localhost ports by design. | Stage 11 | Complete | Focused Stage 11/auth/security `12 passed, 2 warnings`; full backend `199 passed, 2 warnings`; compileall passed; `pnpm web:typecheck`, `pnpm --dir apps/web lint`, `pnpm web:build` passed; `pnpm --dir apps/web audit --audit-level high` clean; `docker compose config --quiet` passed; rebuilt API image `pip check` clean; Trivy production-code scan clean; `pip-audit` one documented protobuf scanner-chain residual. | Stage 11 completion commit |
| Documentation and clean-room final audit | 100% locally achievable with documented residuals | `README.md`, `FEATURE_STATUS.md`, `IMPLEMENTATION_WORKLOG.md`, `docs/CURRENT_STATE_AUDIT.md`, `docs/CAPABILITY_MATRIX.md`, `docs/PHASE_COMPLETION_AUDIT.md`, `docs/TECHNICAL_DEBT.md`, `docs/COMPLETION_MASTER_PLAN.md`, `docs/FINAL_CLEAN_ROOM_AUDIT.md`, `docs/ARCHITECTURE.md`, `docs/SECURITY_MODEL.md`, `docs/DEVELOPMENT.md`, `docs/DEPLOYMENT.md`, `docs/API_REFERENCE.md`, `docs/LOCAL_AI.md`, `docs/DATABASE.md`, `docs/PIPELINE.md`, `docs/SCANNERS.md`, `docs/SANDBOX.md`, `docs/BENCHMARKS.md`, `docs/DESIGN_SYSTEM.md`, `docs/TROUBLESHOOTING.md`, `docs/GITHUB_INTEGRATION.md`, `docs/OPERATIONS.md` | Stage 1-11 regression, benchmark, browser, security, dependency, and Docker suites; final Stage 12 clean-room commands | Fixed stale audit/status docs and cleaned up final evidence. Stage 12 also exposed and fixed a fresh-start migration race, disabled-sandbox runner contact, and Trivy cache/tmpfs pressure in Docker benchmark runs. | Updated canonical docs to actual behavior; removed/classified stale TODO/fake/mock/placeholder/pending/unsupported results; verified documented startup, migrations, service health, static scan, AI-assisted scan, dynamic/ZAP coverage through tests, durable events, findings/dedupe, reports, baselines, drift, cancellation, retry, API/worker/Redis recovery, cross-user denial, benchmarks, backend/frontend/browser/a11y/visual/security/dependency/container tests; recorded local completion, external GitHub activation block, production SaaS limit, residual risks, and final percentages. | `git diff --check`; full backend pytest; frontend lint/typecheck/build/audit; Playwright E2E/a11y/visual; scanner-only benchmark; scanner-plus-Qwen benchmark; Docker clean-room rebuild/up/health; migrations current; dependency/container scans; authenticated health and GitHub blocked-state verification. | Real GitHub activation requires credentials; scanner-plus-Qwen requires local GGUF/GPU/CPU capacity; production SaaS readiness requires deployment infrastructure outside this repo; `pip-audit` reports a documented `protobuf 4.25.9` / `PYSEC-2026-1805` residual. | Stage 12 | Complete | Stage 12 pre-stage SHA `6267b3c6b05c0671d2d311876b3b25f3e4885dd1`; full backend `201 passed, 2 warnings`; `compileall` passed; web typecheck/lint/build/audit passed; Playwright `109 passed, 35 skipped`; clean-room Docker stack rebuilt and healthy; migrations current; Qwen reachable with 28 GPU layers and 4041 MB measured VRAM; scanner-only and scanner-plus-Qwen benchmarks passed with precision/recall/F1 `1.0`; `pip check` clean; Trivy requirements scan clean; `pip-audit` one documented protobuf residual; GitHub status `blocked_missing_credentials`. | Stage 12 completion commit |
