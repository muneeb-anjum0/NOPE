# NOPE Feature Status

Status values are limited to: Complete, Partially complete, Not implemented, Blocked by external dependency, Not applicable.

This matrix was rebuilt during the original Phase 0 on 2026-07-15 and refreshed by the canonical Phase 0 recovery audit on 2026-07-15 from repository evidence, Git history, documentation, implementation files, and the current working tree. A feature is marked Complete only when it is implemented, wired into the real path, exercised, and verified.

Canonical Phase 0 note: current `HEAD` was `194a1e8` (`feat(phase-2): complete scanner execution pipeline`) when recovery began. Phase 3 queue/worker WIP has since been completed, verified, and committed through the canonical phase gate.

| Feature | Current status | Evidence | Relevant files | Missing work | Verification method | External dependency status |
| --- | --- | --- | --- | --- | --- | --- |
| Local authentication | Complete | Local accounts/sessions are stored in Postgres and dashboard routes require a session. | `apps/api/nope_api/auth.py`, `apps/api/nope_api/main.py`, `apps/web/lib/auth.ts`, `apps/web/app/login/page.tsx` | Password reset, org auth, CSRF hardening, production identity provider. | Login smoke tests, Docker health, `GET /api/auth/me`. | None for local mode. |
| User persistence | Complete | `local_users` table is created by migrations and reused across logins. | `apps/api/nope_api/auth.py`, `apps/api/migrations/0001_initial.sql` | Password reset and org identity providers are later SaaS features. | Local login through web/API. | None. |
| Project persistence | Complete | Projects persist in Postgres with owner scoping, supporting target/source/snapshot tables, and authenticated API access. | `apps/api/nope_api/storage.py`, `apps/api/nope_api/main.py`, `apps/api/migrations/0001_initial.sql` | None for Phase 1 local persistence scope. | Persistence tests, authenticated API tests, Docker migration smoke. | None. |
| Scan persistence | Complete | Scans persist in Postgres JSON snapshots and normalized scan rows; Stage 2 durable `scan_events` records every important scan/stage/scanner/retry/cancel/worker/report/Qwen transition with ordered, idempotent replay. | `apps/api/nope_api/storage.py`, `apps/api/migrations/0001_initial.sql`, `apps/api/migrations/0003_scan_events.sql`, `apps/api/nope_api/queue.py` | Later stages can add richer operator analytics, but progress reconstruction is durable. | Stage 2 tests: ordered/idempotent storage, completed-scan backfill, successful/partial/failed/cancelled/retried/stuck-worker timelines, authorization, and pagination. | None. |
| Finding persistence | Complete | Canonical findings, evidence, scanner sources, lifecycle status, recurrence, suppression state, and history rows are written for persisted scans. | `apps/api/nope_api/storage.py`, `apps/api/nope_api/models.py`, `apps/api/migrations/0001_initial.sql` | Later UI phases can expose richer lifecycle controls. | Persistence tests, Phase 4 lifecycle tests, Docker smoke scan. | None. |
| Scanner-run persistence | Complete | Scanner runs persist version/status/coverage/messages/counts, command, exit code, bounded redacted output, and raw artifact links. | `apps/api/nope_api/storage.py`, `apps/api/nope_api/models.py`, `apps/api/migrations/0001_initial.sql` | Authorized artifact download API can be added later for UI convenience. | Backend tests, DB artifact join smoke for `scan_e1b6a69758a848bd`. | None. |
| Scan-stage persistence | Complete | Scan stages persist as ordered rows with name/status/data, and durable `scan_events` preserve the timeline even after browser/API/worker restarts. | `apps/api/nope_api/storage.py`, `apps/api/migrations/0001_initial.sql`, `apps/api/migrations/0003_scan_events.sql`, `apps/api/nope_api/scan_engine.py` | Later observability can add separate aggregate metrics tables. | Stage 2 focused tests and full backend regression. | None. |
| Redis queue | Complete | API enqueues Redis jobs with job IDs, active scan idempotency, retry metadata, bounded backoff, processing tracking, stuck-job requeue support, and queue health. | `apps/api/nope_api/queue.py`, `apps/api/nope_api/main.py`, `apps/api/requirements.txt` | Production queue dashboards/concurrency controls are later polish. | Docker queued scans `scan_4ebe1b771ac7413b`, `scan_c8fbd2708e4044e6`; `/api/queue/status`; Redis outage simulation. | None. |
| Worker consumption | Complete | `nope-worker` consumes Redis jobs, checkpoints progress, persists findings, emits heartbeat, survives Redis loop failures, and uses the shared workspace volume. | `apps/worker/worker.py`, `apps/api/nope_api/queue.py`, `docker-compose.yml`, `docker/api.Dockerfile` | Dedicated worker metrics can be added later. | Docker worker completed queued repository scan with 29 findings; worker heartbeat verified after Redis restart. | None. |
| Retry behavior | Complete | Retry endpoint clears cancellation flags, requeues with force, queue helper tracks attempts/max attempts and bounded backoff, and failed-attempt events are persisted. | `apps/api/nope_api/main.py`, `apps/api/nope_api/queue.py`, `apps/api/tests/test_queue.py` | Attempt analytics UI belongs to later history work. | Failed scan `scan_phase3_retry_failed` retried through API and completed with 29 findings. | None. |
| Cancellation | Complete | Cancel endpoint sets Redis cancellation flag and persists cancellation stages; worker checks cancellation before work and between scan-engine checkpoints. | `apps/api/nope_api/main.py`, `apps/api/nope_api/queue.py`, `apps/api/nope_api/scan_engine.py` | UI cancel controls can be polished later. | Queued scan `scan_c8fbd2708e4044e6` cancelled and stayed cancelled with persisted events. | None. |
| ZIP ingestion | Complete | ZIP uploads extract to bounded temp workspace; size/file count/path traversal/symlink checks exist and UI upload smoke test completed. | `apps/api/nope_api/ingestion.py`, `apps/web/components/scan-launcher.tsx`, `apps/web/app/api/start-scan/route.ts` | Persist uploaded artifact metadata and cleanup records. | `python -m pytest`; UI `/api/start-scan` ZIP smoke test. | None. |
| URL scanning | Partially complete | Authorized URL endpoint performs non-destructive HTTP checks and blocks private/localhost by default. | `apps/api/nope_api/url_scanner.py`, `apps/api/nope_api/security.py`, `apps/api/nope_api/main.py` | TLS depth, browser crawl, privacy tracker detection, authenticated dynamic paths. | URL scope tests and API health. | Browser tooling not integrated yet. |
| Full scan | Complete | Full endpoint validates URL scope, extracts repository uploads, persists repository snapshots, enqueues Redis jobs, worker execution combines repository analysis, sandbox status, URL checks, RAG/Qwen review, coverage, reports, baselines, modified second scans, and drift. | `apps/api/nope_api/main.py`, `apps/api/nope_api/scan_engine.py`, `apps/api/nope_api/queue.py`, `apps/api/nope_api/sandbox.py`, `apps/api/tests/test_phase14_pipeline.py` | None for local full-pipeline scope; Phase 15 UI polish is complete. | Phase 14 full-pipeline tests, full backend suite, frontend lint/typecheck/test/build, Docker queue/worker/API/web/AI smoke. | None for local contracts. |
| Stack detection | Complete for Phase 14 pipeline scope | Heuristic detector identifies common languages/frameworks/data systems from files/manifests and is verified inside the full scan pipeline. | `apps/api/nope_api/stack_detector.py`, `apps/api/tests/test_pipeline.py`, `apps/api/tests/test_phase14_pipeline.py` | Broader ecosystem precision can improve later, but Phase 14 requires wired full-pipeline execution rather than parser perfection. | Phase 14 full-pipeline test asserts stack evidence is produced from uploaded ZIP scans. | None. |
| Attack-surface extraction | Complete for Phase 14 pipeline scope | Heuristic extraction maps common API routes and risky hints, persists them on scans, and feeds code graph/RAG/report views. | `apps/api/nope_api/attack_surface.py`, `apps/api/tests/test_pipeline.py`, `apps/api/tests/test_phase14_pipeline.py` | Deeper middleware/auth/validator modeling can improve later. | Phase 14 full-pipeline test verifies `/invoices/:id` attack-surface extraction from uploaded ZIP scans. | None. |
| Code graph | Complete for Phase 14 pipeline scope | Lightweight route/file/auth/risk graph is generated from attack-surface hints, persisted on scans, and consumed by detail/RAG/report flows. | `apps/api/nope_api/attack_surface.py`, `apps/web/components/attack-map.tsx`, `apps/api/tests/test_phase14_pipeline.py` | Interprocedural AST graph and interactive UI polish remain future quality/UI work. | Phase 14 full-pipeline test verifies code graph nodes are created during worker-executed full scans. | None. |
| Scanner adapters | Complete | Plugin classes execute bundled CLIs, capture redacted raw output, record command/exit code, expose capabilities, normalize supported JSON outputs, and explicitly mark OWASP ZAP baseline skipped/not applicable for repository scans. | `apps/api/nope_api/scanners.py`, `apps/api/nope_api/models.py`, `apps/api/nope_api/main.py` | Broader rule-pack tuning belongs to scanner-quality iteration, not Phase 2 plumbing. | Backend scanner parser tests, `/health`, `/api/scanners/capabilities`, live fixture scan `scan_phase2_verify_20260715_zap_contract`. | None for bundled local repository scanner matrix. |
| Real scanner execution | Complete | API image bundles and runs Semgrep, Gitleaks, OSV-Scanner, Trivy, Checkov, Hadolint, and Bandit; live fixture scan produced normalized findings for every executable repository scanner and persisted raw artifacts. OWASP ZAP baseline is represented in static scanner capabilities and executed only through the Phase 10 sandbox when a safe internal target is declared. | `apps/api/requirements.txt`, `apps/api/nope_api/scanners.py`, `apps/api/nope_api/sandbox.py`, `docker/api.Dockerfile`, `security-packs/semgrep/nope.yml` | Broader scanner quality tuning belongs to later scanner-quality work. | Backend tests, Docker build, `/health`, scanner version checks, fixture scan `scan_phase2_verify_20260715_zap_contract`, Phase 10 ZAP fixture smoke. | None. |
| Result parsing | Complete | Parsers normalize Semgrep, Gitleaks, OSV-Scanner, Trivy, Checkov, Hadolint, and Bandit JSON outputs into shared findings. | `apps/api/nope_api/scanners.py`, `apps/api/tests/test_scanners.py` | SARIF import and additional edge-case fixtures can be added later. | `python -m pytest apps/api/tests`; live scan parsed 28 external scanner findings. | None. |
| Finding normalization | Complete | One canonical finding model now carries scanner run ID, scanner, original rule/severity/confidence, NOPE rule, file/line/symbol/route/endpoint/package/CVE, raw artifact, evidence, attack/impact/remediation, lifecycle, recurrence, baseline, and suppression fields. | `apps/api/nope_api/models.py`, `apps/api/nope_api/rules_engine.py`, `apps/api/nope_api/scanners.py`, `apps/api/nope_api/url_scanner.py` | CVSS-specific enrichment can be added in scanner-quality iterations. | Phase 4 tests and Docker smoke scan `scan_4a6bedf526e1440d`. | None. |
| Deduplication | Complete | Dedupe merges exact fingerprints plus duplicate secrets, CVE/package dependency issues, same file/line, rule/symbol, route/root-cause, code-flow fingerprints, and scanner/source location matches while preserving all evidence. | `apps/api/nope_api/rules_engine.py`, `apps/api/tests/test_phase4_findings.py` | Later graph phases can add deeper interprocedural root-cause grouping. | Phase 4 duplicate secret/CVE/Semgrep+custom/static+dynamic tests. | None. |
| Suppression/false-positive workflow | Complete for canonical backend lifecycle | Finding model persists suppression reason/user/date/expiry/scope; expired suppressions reopen automatically on save. | `apps/api/nope_api/models.py`, `apps/api/nope_api/storage.py`, `apps/api/tests/test_phase4_findings.py` | UI controls for applying suppressions are later UI work. | Suppression expiry unit/integration test. | None. |
| Focused RAG | Complete | Phase 6 adds a bounded graph-aware retriever with repository chunks, functions/classes, routes, imports, database policies, migrations, config, tests, scanner findings, attack-surface nodes, code-graph edges, security guidance, provenance, retrieval reasons, redaction, and prompt-injection controls. | `apps/api/nope_api/rag.py`, `apps/api/nope_api/ai.py`, `apps/api/nope_api/scan_engine.py`, `apps/api/tests/test_phase6_rag.py` | Optional embeddings can be added later, but lexical and graph retrieval work without them. | `python -m pytest apps/api/tests/test_phase6_rag.py apps/api/tests/test_phase5_qwen.py`, `python -m pytest apps/api/tests`, frontend lint/typecheck/build, Docker rebuild/up/health, backend `gitleaks`. | None for Phase 6. |
| Qwen runtime | Complete | `nope-ai` runs llama.cpp CUDA with the host GGUF mounted read-only from `D:\Desktop\Model`; API and worker use canonical `NOPE_QWEN_*` settings and internal endpoint `http://nope-ai:8080`. | `docker-compose.yml`, `docker-compose.ai-cpu.yml`, `docker-compose.ai-gpu.yml`, `docs/LOCAL_AI.md`, `apps/api/nope_api/config.py` | CPU-only performance tuning can be revisited later, but Phase 5 runtime contract is met. | Docker GPU profile, `/health`, model file check, `nvidia-smi`. | 28 GPU layers measured about 4041-4485 MiB VRAM across verification samples; the Phase 16 CUDA-container sample was 4485 MiB, and 30 layers failed to fit, so 28 is the verified cap-safe setting under 5 GB. |
| Qwen inference | Complete | Backend uses llama.cpp `/completion` and OpenAI-compatible `/v1/chat/completions` for focused structured actions; scans preserve deterministic findings if Qwen fails. | `apps/api/nope_api/ai.py`, `apps/api/nope_api/main.py`, `apps/web/components/ai-finding-actions.tsx`, `apps/web/app/api/ai/finding-action/route.ts` | Later RAG phases can add graph/vector retrieval and richer context. | Direct completion, structured chat JSON, FastAPI finding explanation, AI-down scan `scan_443bb3dbdb9b4568`, `python -m pytest apps/api/tests/test_phase5_qwen.py`. | None for Phase 5. |
| Reports JSON/Markdown/SARIF | Complete | Report endpoint renders JSON, Markdown and SARIF-like exports from real scan data; generated report bodies, hashes, media types, and byte sizes persist per scan/format. | `apps/api/nope_api/reports.py`, `apps/api/nope_api/main.py`, `apps/api/nope_api/storage.py`, `apps/api/migrations/0002_report_bodies.sql` | Later reporting polish can add branded templates and export scheduling. | Backend tests, build, API route audit, table-count smoke. | None. |
| PDF reports | Complete | Phase 9 adds real ReportLab PDF rendering with NOPE branding, scope, executive summary, verdict, coverage, scanner status, severity sections, suppressed findings, failed scanners, untested areas, Qwen status, baseline/drift summary, privacy/staging warnings, limitations, methodology, reproducibility metadata, redaction, protected downloads, persisted status, and MinIO artifact metadata. | `apps/api/nope_api/reports.py`, `apps/api/nope_api/storage.py`, `apps/api/nope_api/artifacts.py`, `apps/api/nope_api/main.py`, `apps/api/tests/test_phase9_pdf_reports.py`, `apps/web/app/app/projects/local/reports/page.tsx` | Background report queueing can be added if PDF generation becomes expensive at larger scale; current generation is bounded and synchronous. | Phase 9 PDF tests, full backend suite, frontend lint/typecheck/build, Docker smoke, live PDF download/status check, Gitleaks. | None. |
| Findings filters | Complete | Phase 7 adds server-backed findings query with severity, confidence, status, scanner, rule, CWE, OWASP, file, route, first-seen, lifecycle flags, AI-reviewed, verified, fix-available, query, sorting, and pagination. UI filters are URL-backed and stable on refresh/share. | `apps/api/nope_api/findings.py`, `apps/api/nope_api/main.py`, `apps/web/app/app/projects/local/findings/page.tsx`, `apps/web/components/finding-table.tsx` | Later phases can add saved filter presets. | `python -m pytest apps/api/tests/test_phase7_findings.py`, full backend tests, frontend lint/typecheck/build, Docker API smoke. | None for Phase 7. |
| Finding evidence views | Complete | Finding detail has Overview, Evidence, Code, Code Flow, Fix, Tests, and History tabs; evidence is fetched through authenticated scan ownership and code snippets come from the stored repository workspace with line highlights. | `apps/api/nope_api/findings.py`, `apps/web/app/app/projects/local/findings/page.tsx` | Future artifact download UI can render raw scanner payloads inline when needed. | Phase 7 detail/source/evidence tests and frontend build. | None. |
| Code-flow views | Complete for current graph data | Finding detail shows real graph edges connected to the selected finding file/route and never invents missing flow. | `apps/api/nope_api/findings.py`, `apps/api/nope_api/attack_surface.py`, `apps/web/app/app/projects/local/findings/page.tsx` | Later graph phases can enrich parser-validator-authz-sink paths. | Phase 7 code-flow detail test and UI build. | None. |
| Scan history | Partially complete | Scans page lists persisted user-scoped scans from Postgres. | `apps/web/app/app/projects/local/scans/page.tsx`, `apps/api/nope_api/storage.py` | Filtering, commit metadata enrichment, and scan attempts belong to later queue/history phases. | UI smoke and persistence tests. | None. |
| Baseline comparison | Complete | Phase 8 creates baseline snapshots with scan, commit, repository, target, scanner version, rule version, model, quantization, RAG version, timestamp, coverage, finding, route, dependency, and stack metadata; scans can compare latest vs previous, arbitrary scan vs scan, or scan vs baseline. | `apps/api/nope_api/drift.py`, `apps/api/nope_api/main.py`, `apps/api/nope_api/storage.py`, `apps/api/tests/test_phase8_drift.py`, `apps/web/app/app/projects/local/scans/page.tsx` | Later UI phases can add richer trend charts. | `python -m pytest apps/api/tests/test_phase8_drift.py`, full backend suite, frontend lint/typecheck/build, Docker smoke. | None for Phase 8. |
| Security drift detection | Complete | Drift detects new, fixed, recurring, reintroduced, severity/confidence changes, route/dependency/CVE/secret/security-header/CORS/tracker/public-asset changes, scanner coverage regressions, scanner status changes, stack changes, and conservative incremental scope; Phase 14 verifies baseline plus modified second full scan. | `apps/api/nope_api/drift.py`, `apps/api/nope_api/main.py`, `apps/web/app/app/projects/local/scans/page.tsx`, `apps/api/tests/test_phase14_pipeline.py` | Incremental scope remains advisory; full scans remain the authoritative comparison path. | Phase 8 drift tests plus Phase 14 baseline/modified-second-scan/drift persistence test. | None. |
| Sandbox runner | Complete | Stage 3 moves Docker orchestration behind `nope-runner`; the general worker is non-root and socketless, and manifest-driven disposable Docker workflows keep non-root app containers, read-only repository mounts, no privileged mode, no sandbox Docker socket, no host home, no NOPE secrets, allowlisted images/commands, no arbitrary mounts/env/networks, CPU/memory/process/timeout/tmpfs/log limits, timeout cleanup, unsupported-manifest handling, and scan-stage evidence. | `apps/api/nope_api/sandbox.py`, `apps/api/nope_api/runner.py`, `apps/api/nope_api/scan_engine.py`, `apps/api/nope_api/config.py`, `apps/api/nope_api/main.py`, `docker/api.Dockerfile`, `docker-compose.yml`, `docs/SANDBOX.md` | Browser automation and authenticated dynamic flows remain later scope. | Phase 10 tests plus Stage 3 hostile hardening tests. | Docker daemon access is isolated to the internal `nope-runner`; rootless/separate-host runner can further reduce production residual risk. |
| Dynamic testing | Partially complete | URL scanner does safe non-destructive checks; Phase 10 can start a declared app container on a private internal Docker network and run OWASP ZAP baseline against that isolated target. | `apps/api/nope_api/url_scanner.py`, `apps/api/nope_api/sandbox.py` | Browser crawl, authenticated flow, privacy tracker depth, richer ZAP JSON parsing. | API smoke, Phase 10 unit tests, real ZAP fixture smoke. | ZAP image is pulled from `ghcr.io/zaproxy/zaproxy:stable` for dynamic sandbox scans. |
| Settings persistence | Complete | Phase 11 adds owner-scoped system and project settings APIs, Pydantic validation, persisted Qwen/scanner/default/retention/report/artifact/sandbox settings, encrypted test identity secret storage, no-secret-return responses, audit rows, and settings forms. | `apps/api/nope_api/models.py`, `apps/api/nope_api/settings_contracts.py`, `apps/api/nope_api/storage.py`, `apps/api/nope_api/main.py`, `apps/web/app/app/projects/local/settings/page.tsx`, `apps/web/components/settings-forms.tsx` | Runtime consumption of every scanner override can deepen in later scanner policy work; Phase 11 persistence and contracts are complete. | Phase 11 API tests, full backend suite, frontend lint/typecheck/build, Docker smoke. | None for local settings contracts. |
| Scanner health/capability reporting | Complete | `/health` reports command availability and `/api/scanners/capabilities` returns installed status, version, coverage categories, and supported markers; Phase 11 persists scanner enabled/timeout policy state. | `apps/api/nope_api/scanners.py`, `apps/api/nope_api/main.py`, `apps/api/nope_api/settings_contracts.py` | Per-project scanner policy enforcement can deepen in later scanner policy work. | `GET /health`; authenticated `GET /api/scanners/capabilities`; Phase 11 settings tests. | None. |
| GitHub contracts | Complete | Stage 9 completes owner-scoped GitHub status/settings/connect/callback/repositories/disconnect routes, encrypted App/OAuth/token credential storage, CSRF state validation, repository reference persistence, settings UI controls, and honest blocked/revoked states. | `apps/api/nope_api/github.py`, `apps/api/nope_api/main.py`, `apps/api/nope_api/storage.py`, `apps/api/nope_api/settings_contracts.py`, `apps/api/tests/test_phase11_settings_github.py`, `apps/api/tests/test_stage9_github_integration.py`, `apps/web/app/app/projects/local/settings/page.tsx` | Real github.com OAuth code exchange and PR patch automation remain external/later scope unless production credentials are supplied and verified. | Stage 9 fake-GitHub protocol tests plus Phase 11 compatibility tests. | Real private access requires operator-supplied GitHub credentials. |
| GitHub private access | Locally complete / externally blocked until credentials exist | NOPE can verify a stored token against the GitHub API, list repositories, detect default branch and commit SHA, download an archive without credential-bearing remotes, create repository sources/snapshots, and queue scans. Without valid credentials it remains blocked and returns no fake repositories. | `apps/api/nope_api/github.py`, `apps/api/nope_api/main.py`, `apps/api/nope_api/storage.py`, `apps/api/tests/test_stage9_github_integration.py` | Live github.com private repo access must be tested when real credentials are available; PR workflow is not implemented because it is not activated in current product scope. | Controlled local fake GitHub server tests verify the protocol without mocks of success. | Requires real GitHub credentials for external activation. |
| MinIO artifact storage | Complete | Scanner raw stdout/stderr payloads and generated PDF reports are stored in MinIO and linked through `uploaded_artifacts`, `job_artifacts`, scanner runs, and report metadata. | `apps/api/nope_api/artifacts.py`, `apps/api/nope_api/storage.py`, `docker-compose.yml` | Authorized artifact download route can be added when the UI needs direct raw-output viewing. | DB join and MinIO object listing for scanner artifacts; Phase 9 PDF status smoke verifies report artifact metadata. | None. |
| Audit logs | Partially complete | Phase 1 schema and repository method can persist audit rows; application event coverage is not wired broadly yet. | `apps/api/nope_api/storage.py`, `apps/api/migrations/0001_initial.sql` | Wire auth, project, scan, settings, report events through audit logging. | Phase 1 contract entity tests. | None. |
| Benchmarks | Complete | Phase 12 / Stage 1 now has a versioned benchmark fixture covering all 41 required vulnerability categories, versioned expected output, one CLI command, scanner-only and scanner-plus-Qwen modes, machine-readable metrics, true-positive/false-positive/known-false-negative comparison, scan duration, resource use, per-category/per-scanner metrics, scanner failure visibility, Qwen contribution, and fix-verification fields. | `apps/api/nope_api/benchmarks.py`, `benchmarks/fixtures/nope-benchmark-v1`, `benchmarks/expected/nope-benchmark-v1.expected.json`, `docs/BENCHMARKS.md`, `apps/api/tests/test_phase12_benchmarks.py` | None for the locally achievable Stage 1 benchmark scope; later scanner-quality stages may add more scanner families and deeper fixtures. | Phase 12 benchmark tests, scanner-only CLI, scanner-plus-Qwen CLI, full backend suite, canonical Docker benchmarks. | Scanner-plus-Qwen CI remains external because CI has no local GGUF/GPU mount. |
| Tests | Complete | Phase 13 expands backend tests across unit, integration, E2E, and NOPE-security contracts, including migrations, scan states, queue serialization, scanner applicability and command construction, parsers, severity normalization, fingerprints, dedupe, suppression expiry, drift, RAG, secret redaction, AI output parsing/fallback, PDF, sandbox policies, settings validation, benchmark schema, login/project/ZIP scan flow, findings detail, evidence, report download, baseline comparison, ownership, malformed ZIP, SSRF/private URL blocking, bearer-only CSRF posture, and login rate limiting. Frontend has CI-compatible lint/typecheck/test/build commands, and Phase 15 added browser viewport verification. | `apps/api/tests/test_phase13_expansion.py`, existing `apps/api/tests/*`, `apps/web/package.json`, `docs/TESTING.md` | Formal axe/Playwright E2E automation can still deepen later, but Phase 15 viewport polish is complete. | Phase 13 focused tests, full backend suite, frontend lint/typecheck/test/build, Docker smoke, Phase 15 66-check browser viewport matrix. | GPU/Qwen tests are optional and separate from the core suite. |
| Frontend UI | Complete | Phase 15 delivers the required public landing page, separate `/app` dashboard, graphite-only visual system, React Bits-inspired LineSidebar, polished dashboard/finding/attack-map/coverage/scans/assets/reports/settings routes, custom controls, motion states, reduced-motion support, and responsive mobile adaptation. | `apps/web/app/page.tsx`, `apps/web/app/app/projects/local/page.tsx`, `apps/web/app/app/projects/local/findings/page.tsx`, `apps/web/components/app-shell.tsx`, `apps/web/components/line-sidebar.tsx`, `apps/web/app/globals.css` | Formal axe automation and richer browser E2E suites can be added later; no Phase 15 requirement remains open. | Frontend lint/typecheck/test/build; browser matrix across `/`, `/login`, `/app`, and all app routes at 1440, 1280, 1024, 768, 390, and 360 widths. | None. |
| Documentation | Complete | Phase 16 refreshes README, status, worklog, reconciliation, architecture, security, development, deployment, API, local AI, database, pipeline, scanners, sandbox, benchmarks, design system, and troubleshooting documentation. | `README.md`, `docs/ARCHITECTURE.md`, `docs/SECURITY_MODEL.md`, `docs/DEVELOPMENT.md`, `docs/DEPLOYMENT.md`, `docs/API_REFERENCE.md`, `docs/LOCAL_AI.md`, `docs/DATABASE.md`, `docs/PIPELINE.md`, `docs/SCANNERS.md`, `docs/SANDBOX.md`, `docs/BENCHMARKS.md`, `docs/DESIGN_SYSTEM.md`, `docs/TROUBLESHOOTING.md`, `docs/FEATURE_STATUS.md`, `docs/PHASE_RECONCILIATION.md`, `docs/IMPLEMENTATION_WORKLOG.md` | None for Phase 16 documentation scope. | Docs audit, keyword cleanup sweep, full backend/frontend/Docker/security verification. | None. |
| Docker core stack | Complete | Web/API/worker/Postgres/Redis/MinIO start; primary container is named `NOPE`; health checks pass. | `docker-compose.yml`, `docker/api.Dockerfile`, `docker/web.Dockerfile` | Production gateway/resource limits/scanner jobs. | `docker compose up --build -d`, `docker compose ps`. | None. |
| Docker AI profile | Complete | GPU profile uses `ghcr.io/ggml-org/llama.cpp:server-cuda`, read-only GGUF mount, internal service URL, health check, and bounded Qwen context/concurrency/output settings. CPU fallback profile disables visible NVIDIA devices and sets GPU layers to 0. | `docker-compose.yml`, `docker-compose.ai-cpu.yml`, `docker-compose.ai-gpu.yml` | Later deployment work can add production resource quotas. | Docker AI profile, `/health`, direct inference, structured inference, FastAPI explanation, AI-down scan, `nvidia-smi`. | 28 GPU layers is verified under 5 GB; CPU fallback is configured/documented. |
| Resource controls | Partially complete | Archive/file/time/AI limits are configured; scanner subprocess timeout, whole-scan worker timeout, retry cap, bounded backoff, and Phase 10 sandbox CPU/memory/PID/tmpfs/log/timeout limits exist. | `apps/api/nope_api/config.py`, `apps/api/nope_api/ingestion.py`, `apps/api/nope_api/scanners.py`, `apps/api/nope_api/queue.py`, `apps/api/nope_api/sandbox.py` | Concurrency limits, artifact quotas, per-project settings. | Tests, Docker queue proof, real Docker sandbox abuse checks. | None. |
| Observability | Partially complete | Scan IDs, stages, scanner statuses, queue status, scan events, worker heartbeat, and redacted worker failure messages exist. | `apps/api/nope_api/models.py`, `apps/api/nope_api/scan_engine.py`, `apps/api/nope_api/queue.py` | Structured logs, request IDs, metrics export. | `/api/scans/{id}/events`, `/api/queue/status`, Docker logs. | None. |
| Accessibility/responsive UI | Complete for Phase 15 scope | Keyboard sidebar, focus-visible states, styled controls, reduced motion, mobile sidebar adaptation, responsive app grids, table containment, and mobile attack-map layout are implemented. | `apps/web/app/globals.css`, `apps/web/components/line-sidebar.tsx`, `apps/web/components/app-shell.tsx` | Formal axe automation can be added later, but Phase 15's reasonable accessibility/responsive acceptance criteria are met. | Browser matrix verified 66 route/viewport combinations with styles loaded, no horizontal overflow, and no offscreen rendered elements. | None. |

## Phase 1 Completed

Phase 1 objective: replace the in-memory project/scan/finding/report state with migration-backed PostgreSQL persistence, authenticated ownership scoping, and durable generated report storage.

Phase 1 acceptance targets:

- Migrations create durable tables for users/sessions, projects, targets, repository sources/snapshots, scans, stages, scanner runs, findings, evidence, sources, history, coverage, reports, model/scanner/settings, baselines, drift events, artifacts, audit logs, and GitHub contract entities.
- The normal scan path persists project, scan, finding, coverage, stage, scanner-run, and generated report payloads to Postgres.
- API restart does not lose scans.
- Local auth remains functional.
- Existing tests continue to pass, with new persistence tests added.

## Phase 1 Result

Phase 1 status: Complete for local persistence scope.

Verification evidence:

- Added SQL migration runner, migration status reporting, initial schema migration, and report-body migration.
- Added durable tables for local auth, projects, targets, repository sources/snapshots, scans, stages, scanner runs, findings, evidence, sources, history, coverage, reports, settings, baselines, drift events, artifacts, audit logs, and GitHub contract entities.
- Replaced `InMemoryStore` with `PostgresStore`.
- Added persistence/auth tests; backend test count is now 16.
- Verified a ZIP scan through the web route persisted and remained readable after `nope-api` restart.
- Verified normalized table rows exist for scans, stages, scanner runs, findings, coverage, reports, and schema migrations.
- Protected API endpoints now require a local bearer token by default and are user-scoped.
- Generated JSON, Markdown, and SARIF report bodies persist in Postgres with body hashes and byte counts.

Phase 1 is closed. Redis-backed queued scan execution remains a later worker phase; scanner execution and artifact handling are now closed in Phase 2.

## Phase 2 Completed

Phase 2 objective: make external scanner execution real enough to produce normalized findings and durable scanner-run evidence without faking unavailable tools.

Phase 2 result:

- Added normalized JSON parsers for Semgrep, Gitleaks, OSV-Scanner, Trivy, Checkov, Hadolint, and Bandit.
- Added command, exit-code, redacted stdout, and redacted stderr fields to scanner runs.
- Added bounded scanner output storage through `NOPE_MAX_SCANNER_OUTPUT_BYTES`.
- Bundled Semgrep, Gitleaks, OSV-Scanner, Trivy, Checkov, Hadolint, and Bandit in the API image.
- Added local Semgrep rules under `security-packs/semgrep/nope.yml`.
- Added scanner parser, capability, and scanner execution artifact tests.
- Added MinIO raw scanner artifact storage with `uploaded_artifacts`, `job_artifacts`, and `scanner_runs.raw_artifact_id` links.
- Added authenticated `/api/scanners/capabilities` with scanner versions, coverage categories, and supported markers.
- Added `docs/SCANNERS.md`.
- Verified Docker API image reports all bundled scanners installed.
- Verified scanner versions: Semgrep `1.169.0`, Bandit `1.9.4`, Gitleaks `8.28.0`, OSV-Scanner `2.2.3`, Trivy `0.72.0`, Checkov `3.3.8`, Hadolint `2.14.0`.
- Verified authenticated ZIP scan `scan_e1b6a69758a848bd` completed with 29 findings: NOPE rules 1, Semgrep 1, Gitleaks 1, OSV-Scanner 5, Trivy 10, Checkov 6, Hadolint 2, Bandit 3.
- Verified MinIO object storage and DB artifact joins for all seven external scanner raw-output artifacts in `scan_phase2_verify_20260715_zap_contract`.

Phase 2 status: Complete for the bundled repository scanner execution and evidence pipeline. OWASP ZAP baseline is explicitly present in the scanner contract and skipped/not applicable for repository scans.

Out of scope for Phase 2 and still tracked separately: Redis worker queue, real ZAP/browser dynamic testing against an internal target, Qwen inference, suppression workflow, and PDF reports.

## Phase 3 Completed

Phase 3 objective: move scan execution behind a real Redis queue and worker while preserving durable progress, cancellation, retry, and restart behavior.

Phase 3 result:

- API scan start endpoints now validate and persist queued scans, then return before scan execution.
- Repository/full scans extract ZIP uploads into the shared `/app/.nope-workspaces` Docker volume for worker access.
- Redis jobs include job IDs, active-scan idempotency, attempts, maximum attempts, and run-after timestamps.
- Worker consumes Redis jobs, persists stage checkpoints, writes final findings/scanner runs, emits heartbeat, and reconnects after Redis loop failures.
- Cancellation is requested through Redis flags and checked before work plus between scan-engine checkpoints.
- Retry endpoint clears cancellation flags and requeues failed/partial/cancelled/completed scans when the workspace is available.
- Queue status, worker health, and scan events endpoints expose reload-safe progress.
- Docker image/volume permissions now allow the non-root API and worker users to share repository workspaces.

Verification evidence:

- `python -m pytest apps/api/tests`: 29 passed.
- Docker images refreshed: API `903026c0b97a`, worker `bda4f232c33b`.
- Queued two scans through the real API with the worker stopped; both returned `queued` immediately.
- Restarted API before worker execution; worker later completed `scan_4ebe1b771ac7413b` with 29 findings.
- Cancelled queued scan `scan_c8fbd2708e4044e6`; worker persisted `cancelled` without running scanners.
- Interrupted worker during `scan_6ede29d9d13d4778`, requeued the processing payload, and completed the scan after worker restart with 29 findings.
- Retried failed scan `scan_phase3_retry_failed` through the API retry endpoint; worker completed it with 29 findings.
- Stopped Redis; `/api/queue/status` returned `redis:error`, and the rebuilt worker stayed alive and restarted its queue loop after Redis returned.

Phase 3 status: Complete for Redis-backed local queue and worker orchestration.

## Phase 4 Completed

Phase 4 objective: use one canonical finding schema everywhere, merge duplicate findings, preserve all evidence, retain original scanner severity, and persist lifecycle/recurrence/suppression behavior.

Phase 4 result:

- Expanded the canonical `Finding` model with scanner/run/rule metadata, original severity/confidence, source location, route/endpoint, package/CVE, raw artifact, attack/impact, lifecycle, AI review, recurrence, baseline, and suppression fields.
- Added `FindingStatus`, `BaselineState`, `Suppression`, and `Confidence.uncertain`.
- Updated NOPE rule, external scanner, and URL scanner normalization to populate canonical metadata.
- Replaced exact-fingerprint-only dedupe with correlation keys for duplicate secrets, CVE/package dependency findings, same file/line, rule/symbol, route/root cause, code-flow fingerprint, dependency finding, and scanner/source location.
- Dedupe now preserves every unique evidence item and promotes normalized severity/confidence while keeping original scanner severity/confidence.
- Store save now enriches findings before snapshot persistence, automatically reopens expired suppressions, computes recurrence count, and marks reintroduced findings when prior history shows fixed/verified.

Verification evidence:

- `python -m pytest apps/api/tests`: 36 passed.
- Docker images refreshed: API `e48f0d44740f`, worker `ba5d2472e0a4`.
- Docker stack healthy: web, API, worker, Postgres, Redis, MinIO.
- Worker smoke scan `scan_4a6bedf526e1440d` completed with 25 deduped findings, down from the previous 29 raw scanner/custom findings because duplicates now merge.
- Returned finding JSON includes canonical fields such as `scanner`, `original_rule_id`, `original_severity`, `start_line`, `end_line`, `verification_state`, `ai_review_state`, `recurrence_count`, and `baseline_state`.
- Phase 4 tests verify duplicate secret, duplicate CVE/package, Semgrep plus custom-rule merge, static plus dynamic route merge, severity/confidence mapping, suppression expiry, and reintroduced recurrence.

Phase 4 status: Complete for backend canonical finding normalization, deduplication, correlation, lifecycle, suppression expiry, and recurrence.
